import React from "react";

import styles from "./SignInWithEmailLine.module.scss";

/**  Or Sign with Email line */
function SignInWithEmailLine() {
  return (
    <p className={styles.ptag}>
      <span>or Sign in with Email</span>
    </p>
  );
}

export default SignInWithEmailLine;
